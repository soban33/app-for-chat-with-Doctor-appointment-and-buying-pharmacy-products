<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soban.draslaafun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrAslaafUn">
        <activity android:name=".Activity_Cart"></activity>
        <activity android:name=".Activity_EnlargeImageAsDialogue" />
        <activity android:name=".Activity_chatWithDoctor" />
        <activity android:name=".Activity_Login" />
        <activity android:name=".Activity_SignUp" />
        <activity android:name=".Activity_PharmacyProdList" />
        <activity android:name=".Activity_AboutDoctor" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>